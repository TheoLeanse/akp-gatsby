import React from 'react'
import Link from 'gatsby-link'

const Header = ({ siteTitle }) => (
  <header>
    <span class="title">Anna Paterson </span>
    <a class="contact" href="info">
      Info
    </a>
  </header>
)

export default Header
